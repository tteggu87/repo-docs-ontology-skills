metadata:
  status: Active
  source_of_truth: false
  last_updated: YYYY-MM-DD
  purpose: "Named entities referenced by actions, datasets, and current repository guidance."

entities:
  - key: sample.entity
    description: "Replace with the canonical entity description."
    canonical_terms:
      - sample.term
    owned_by: platform
    used_by_actions:
      - sample.action
