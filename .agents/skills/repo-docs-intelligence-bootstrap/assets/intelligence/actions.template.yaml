metadata:
  status: Active
  source_of_truth: false
  last_updated: YYYY-MM-DD
  purpose: "Current action contracts that reduce drift between declared behavior, capability bindings, and active implementations."

actions:
  - key: sample.action
    status: implemented
    description: "Replace with the real action contract and intent."
    capability: cli.sample_action
    implementation: "package.module:function"
    writes_layers:
      - Raw
    touches_datasets:
      - primary.store
    notes:
      - "Keep this action narrowly scoped and reusable."
      - "Update this contract before changing the bound implementation when behavior changes."
