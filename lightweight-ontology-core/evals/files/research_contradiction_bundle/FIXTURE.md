# Fixture: Research Contradiction Bundle

This fixture keeps contradiction handling intentionally narrow.
It includes:

- one explicit `contradicts` claim
- one exclusive-object conflict under `classifies_as`
- one unresolved disputed claim

The validator should flag these without pretending semantic reconciliation is automatic.
