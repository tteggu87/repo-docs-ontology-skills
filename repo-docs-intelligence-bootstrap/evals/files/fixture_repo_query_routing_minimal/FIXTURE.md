# Fixture: Query Routing Minimal

This fixture validates the lightweight route contract bundle.
It includes:
- `intelligence/manifests/routes.yaml`
- `intelligence/policies/query-routing.yaml`
- `warehouse/jsonl/query_receipts.jsonl`

The validator should accept this bundle without hard routing-contract errors.
