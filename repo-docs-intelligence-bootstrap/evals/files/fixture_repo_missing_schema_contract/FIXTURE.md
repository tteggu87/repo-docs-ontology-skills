# Fixture Repo: Missing Schema Contract

This repo already has Python implementation and action/capability links for `generate_v_atomic`.
What is missing is the lightweight ontology around it: glossary terms, policy/gate semantics, and schema or dataset contracts.
The goal is to force schema-first alignment instead of documenting only the Python path.

Key paths:

- `pkg_intelligence/embedding_service.py`
- `pkg_intelligence/capabilities.py`
- `intelligence/manifests/actions.yaml`
- `intelligence/registry/capabilities.yaml`
